# RegEx Tricks and Hacks

```
'(important text starts)(.+)((?:\n.+)+)(important text ends)'
```
Matches all **multi-line** text that starts and ends with the substrings provided. Super useful for fast JSON,YAML parsing.


